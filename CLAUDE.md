# منصة تحفيز طلاب مجمع رياض القرآن التعليمي

منصة نقاط وشيكات تحفيزية لحلقات تحفيظ القرآن: يرصد المشرف النقاط بمسح باركود الطالب
أو بإصدار شيكات، وتظهر النتائج في شاشات فارس الأسبوع وحلقة الأسبوع والصدارة، ويستبدل
الطلاب نقاطهم من المتجر. واجهة عربية RTL تعمل على الجوال والآيباد والحاسب وقابلة
للتثبيت كتطبيق (PWA).

## الأوامر

```bash
npm install          # تثبيت الحزم
npm start            # تشغيل الخادم (المنفذ 3000 افتراضياً)
npm run dev          # تشغيل مع إعادة التحميل عند التعديل
npm test             # اختبارات واجهات البرمجة (node --test)
npm run seed         # بيانات تجريبية: حلقات وطلاب ونقاط وجوائز
npm run icons        # توليد أيقونات التطبيق من public/img/logo.jpg (يتطلب Pillow)
```

بعد `npm start` افتح <http://localhost:3000>. أول تشغيل يُنشئ حساب مدير ويطبع بياناته
في الطرفية (رقم الجوال `0500000000` والرمز المؤقت `1234` ما لم يُحدَّد `ADMIN_PHONE`).

متغيرات البيئة: `PORT` · `DATA_DIR` · `ADMIN_PHONE` · `ADMIN_NAME` · `COOKIE_SECURE` · `TZ`.

## البنية

```
server/            خادم Express + SQLite (node:sqlite، وبديله better-sqlite3 على Node الأقدم)
  db.js            المخطط والترقيات والإعدادات الافتراضية
  auth.js          تشفير الرموز والجلسات وحُرّاس الصلاحيات
  stats.js         حساب النقاط والصدارة وفارس الأسبوع وحلقة الأسبوع
  catalog.js       دفاتر الشيكات وقيم بنودها
  routes/          auth · students · halaqat · points (+scan) · cheques · store · screens · settings
public/            واجهة بلا أدوات بناء: وحدات ES مباشرة
  index.html       تطبيق المشرف والطالب (توجيه بالـ hash داخل js/app.js)
  js/views/*.js    شاشة لكل ملف: render() تُعيد HTML و mount() تربط الأحداث
  js/student-card.js  بطاقة الطالب والباركود ووضع العرض للمسح
  screen.html      شاشة العرض في القاعة · print.html الشيكات · cards.html البطاقات
  sw.js            عامل الخدمة (PWA) — حدّث قائمة الملفات ورقم النسخة عند إضافة ملف
data/              قاعدة البيانات والصور المرفوعة (خارج Git — انسخها احتياطياً)
```

## قواعد المشروع

- **الواجهة عربية بالكامل**: كل نص يراه المستخدم بالعربية، والتعليقات في الكود بالعربية أيضاً.
- **لا أدوات بناء ولا إطارات**: وحدات ES ووظائف مساعدة في `public/js/ui.js`. عند إدراج
  رقم داخل جملة عربية استخدم `nb()` لعزله اتجاهياً.
- **النقاط من فئة ٢٥** وتساوي الريال في الشيك. قيم البنود من الإعدادات، لا تكتبها في الكود.
- **الدخول برقم الجوال + رمز**: الرمز المؤقت `1234` من الإعدادات، وبعد أول دخول تُفرض
  شاشة تغيير الرمز، والمنع مطبَّق في الخادم (`must_change_code`) لا في الواجهة فقط.
- **الباركود** (`RQ00012`) للرصد فقط ومستقل عن رقم الجوال.
- عند إضافة ملف واجهة جديد: أضِفه إلى قائمة `SHELL` في `public/sw.js` وارفع رقم النسخة.
- شغّل `npm test` قبل أي رفع؛ الاختبارات تغطي الشيكات والمسح والمتجر والصلاحيات والدخول.

## النشر

التفاصيل في `DEPLOY.md`. جاهز: `Dockerfile` · `fly.toml` · `render.yaml` ·
`deploy/install.sh` (خادم Linux بأمر واحد) · `app.js` (لاستضافات cPanel/Passenger).
كل رفعة تشغّل اختبارات GitHub Actions تلقائياً.
